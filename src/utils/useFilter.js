const useFilter = (_id, relIDCol, counterpathCol) => {
    const collectionOfTargetContactID = relIDCol.filter(item => item.conID === contact_ID) // set up parameter of rel collection properly and standard
}